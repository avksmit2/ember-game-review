
import { gameRating } from 'game-review/helpers/game-rating';
import { module, test } from 'qunit';

module('Unit | Helper | game rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = gameRating([42]);
  assert.ok(result);
});

