
import { numberReviews } from 'game-review/helpers/number-reviews';
import { module, test } from 'qunit';

module('Unit | Helper | number reviews');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = numberReviews([42]);
  assert.ok(result);
});

